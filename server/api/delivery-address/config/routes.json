{
  "routes": [
    {
      "method": "GET",
      "path": "/delivery-addresses",
      "handler": "delivery-address.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-addresses/count",
      "handler": "delivery-address.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-addresses/:id",
      "handler": "delivery-address.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/delivery-addresses",
      "handler": "delivery-address.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/delivery-addresses/:id",
      "handler": "delivery-address.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/delivery-addresses/:id",
      "handler": "delivery-address.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
